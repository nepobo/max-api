# 🔧 Инструкция по установке и запуску (Linux)

## Проблема: externally-managed-environment

На современных системах Linux (Debian/Ubuntu) Python управляется системой, поэтому нужно использовать виртуальное окружение.

## Решение 1: Установка через виртуальное окружение (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Установите python3-venv

```bash
sudo apt update
sudo apt install python3.12-venv python3-full -y
```

### Шаг 2: Создайте виртуальное окружение

```bash
cd /home/nepobo/Myprojects/max-api
python3 -m venv venv
```

### Шаг 3: Активируйте виртуальное окружение

```bash
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 4: Установите зависимости

```bash
pip install -r requirements.txt
```

### Шаг 5: Создайте файл .env с токеном

```bash
cp .env.example .env
nano .env  # или используйте любой редактор
```

Добавьте ваш токен:
```env
MAX_BOT_TOKEN=ваш_токен_здесь
```

### Шаг 6: Запустите приложение

```bash
python -m console_app.main
```

Или запустите примеры:
```bash
python examples/echo_bot.py
python examples/simple_bot.py
```

### Деактивация виртуального окружения

Когда закончите работу:
```bash
deactivate
```

---

## Решение 2: Использование pipx (альтернатива)

Если не хотите создавать venv вручную:

```bash
# Установка pipx
sudo apt install pipx -y
pipx ensurepath

# Использование через pipx
cd /home/nepobo/Myprojects/max-api
python3 -m pip install --user -r requirements.txt
```

---

## Решение 3: Использование Poetry (продвинутый вариант)

```bash
# Установка Poetry
curl -sSL https://install.python-poetry.org | python3 -

# Инициализация проекта
cd /home/nepobo/Myprojects/max-api
poetry init
poetry add requests python-dotenv click colorama pydantic

# Запуск
poetry run python -m console_app.main
```

---

## Полная последовательность команд (copy-paste ready)

```bash
# 1. Установка необходимых пакетов
sudo apt update && sudo apt install python3.12-venv python3-full -y

# 2. Переход в директорию проекта
cd /home/nepobo/Myprojects/max-api

# 3. Создание виртуального окружения
python3 -m venv venv

# 4. Активация виртуального окружения
source venv/bin/activate

# 5. Установка зависимостей
pip install -r requirements.txt

# 6. Создание конфигурационного файла
cp .env.example .env
echo "Теперь отредактируйте .env и добавьте ваш токен бота"
echo "nano .env"

# 7. После добавления токена запустите приложение
# python -m console_app.main
```

---

## Проверка установки

После активации venv и установки зависимостей проверьте:

```bash
# Проверка Python
which python
# Должно быть: /home/nepobo/Myprojects/max-api/venv/bin/python

# Проверка установленных пакетов
pip list

# Проверка импорта библиотеки
python -c "from max_api import MAXClient; print('OK')"
```

---

## Быстрый тест без токена

Протестируйте утилиты без токена:

```bash
source venv/bin/activate
python -c "
from max_api.utils import build_inline_keyboard, validate_chat_id

# Тест валидации
print('Тест validate_chat_id:', validate_chat_id(123456))

# Тест создания клавиатуры
keyboard = build_inline_keyboard([
    [{'type': 'callback', 'text': 'Кнопка', 'payload': 'test'}]
])
print('Тест клавиатуры:', keyboard['type'])
print('Все тесты пройдены!')
"
```

---

## Запуск тестов

После установки зависимостей:

```bash
source venv/bin/activate
pytest tests/ -v
```

---

## Troubleshooting

### Ошибка: "python3-venv not found"

Попробуйте установить конкретную версию:
```bash
sudo apt install python3.12-venv
# или
sudo apt install python3.11-venv
```

### Ошибка: "pip not found" после активации venv

```bash
python -m ensurepip --upgrade
pip install --upgrade pip
```

### Ошибка: "Permission denied"

Убедитесь, что используете venv:
```bash
which python  # должен показать путь к venv/bin/python
```

---

## Структура после установки

```
max-api/
├── venv/                    # Виртуальное окружение (создано)
│   ├── bin/
│   │   ├── python
│   │   ├── pip
│   │   └── activate
│   └── lib/
├── max_api/                 # Библиотека
├── console_app/             # Консольное приложение
├── examples/                # Примеры
├── tests/                   # Тесты
├── .env                     # Ваш конфиг (создайте!)
└── requirements.txt
```

---

## Дополнительные команды

### Обновление зависимостей

```bash
source venv/bin/activate
pip install --upgrade -r requirements.txt
```

### Очистка venv и пересоздание

```bash
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Экспорт установленных пакетов

```bash
source venv/bin/activate
pip freeze > requirements-lock.txt
```

---

## Готово! 🎉

После выполнения всех шагов вы сможете:
- ✅ Запускать консольное приложение
- ✅ Использовать примеры ботов
- ✅ Разрабатывать собственных ботов
- ✅ Запускать тесты

**Следующий шаг:** Получите токен бота на https://business.max.ru/self

# Правила для ИИ-агента: MAX Messenger API Client

## Обзор проекта
Это Python-библиотека и консольное приложение для работы с MAX Messenger API.
- Язык: Python 3.8+
- Главный модуль: `max_api/`
- Примеры: `examples/`
- Консольное приложение: `console_app/`

## Критические правила работы с MAX API

### 1. Отправка сообщений ⚠️
**ВСЕГДА** используй `user_id` как query-параметр в URL:
```python
# ПРАВИЛЬНО ✅
POST /messages?user_id=12374848
Body: {"text": "Hello"}

# НЕПРАВИЛЬНО ❌
POST /messages
Body: {"recipient": {"chat_id": 12374848}, "text": "Hello"}
```

### 2. Получение chat_id из обновлений ⚠️
**ВСЕГДА** используй `sender.user_id` для ответа на личные сообщения:
```python
# ПРАВИЛЬНО ✅
chat_id = message['sender']['user_id']

# НЕПРАВИЛЬНО ❌
chat_id = message['recipient']['chat_id']
```

### 3. Long Polling timeout ⚠️
Timeout при Long Polling - это **НОРМАЛЬНОЕ** поведение, не ошибка!
```python
# ПРАВИЛЬНО ✅
try:
    updates = client.get_updates(timeout=30, marker=last_marker)
except Exception as e:
    if "timeout" in str(e).lower() or "превышено время ожидания" in str(e):
        continue  # Просто продолжаем цикл
    else:
        # Настоящая ошибка - логируем
        print(f"Ошибка: {e}")
```

### 4. Аутентификация
- Токен передаётся через заголовок `Authorization: {token}`
- **НЕ** используй префикс "Bearer"
- Токен хранится в `.env` файле как `MAX_BOT_TOKEN`

## Структура проекта

```
max-api/
├── max_api/               # Основная библиотека
│   ├── __init__.py       # Экспорты (MAXClient, exceptions)
│   ├── client.py         # MAXClient - главный класс
│   ├── exceptions.py     # Исключения API
│   └── utils.py          # Утилиты (RateLimiter, validate_chat_id, etc)
├── examples/             # Примеры ботов
│   ├── echo_bot.py      # Эхо-бот
│   ├── simple_bot.py    # Бот с командами
│   ├── get_chat_id_bot.py  # Получение chat_id
│   └── keyboard_example.py # Inline клавиатуры
├── console_app/          # Интерактивное CLI
│   ├── main.py          # Главное меню
│   └── config.py        # Конфигурация
├── tests/                # Тесты pytest
└── docs/                 # Документация
```

## Правила кодирования

### При редактировании max_api/client.py:
1. **НИКОГДА** не меняй формат отправки сообщений (user_id в query)
2. Сохраняй RateLimiter на 30 RPS
3. Все методы должны возвращать dict или raise MAXAPIException
4. Используй `self.session` для всех HTTP запросов

### При создании новых ботов:
```python
# Шаблон бота
import sys
import os
import time
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from max_api import MAXClient
from dotenv import load_dotenv

load_dotenv()

def main():
    token = os.getenv('MAX_BOT_TOKEN')
    if not token:
        print("Ошибка: токен не найден")
        return
    
    client = MAXClient(token=token)
    bot_info = client.get_me()
    print(f"Бот '{bot_info['name']}' запущен!")
    
    last_marker = None
    
    try:
        while True:
            try:
                updates = client.get_updates(timeout=30, marker=last_marker)
            except Exception as e:
                if "timeout" in str(e).lower():
                    continue
                print(f"Ошибка: {e}")
                time.sleep(5)
                continue
            
            for update in updates:
                if update.get('update_type') == 'message_created':
                    message = update['message']
                    chat_id = message['sender']['user_id']
                    text = message.get('body', {}).get('text', '')
                    
                    # Твоя логика здесь
                    client.send_message(chat_id=chat_id, text="Ответ")
                
                if 'marker' in update:
                    last_marker = update['marker']
    
    except KeyboardInterrupt:
        print("\nБот остановлен")
    finally:
        client.close()

if __name__ == "__main__":
    main()
```

## Методы MAXClient

### Основные методы:
- `get_me()` - информация о боте
- `send_message(chat_id, text, attachments=None, format=None)` - отправка
- `get_updates(timeout=30, marker=None)` - получение обновлений
- `create_subscription(url)` - webhook
- `get_subscriptions()` - список webhook'ов
- `delete_subscription(subscription_id)` - удалить webhook

### Параметры send_message:
```python
client.send_message(
    chat_id=12374848,              # ОБЯЗАТЕЛЬНО: user_id получателя
    text="Hello *world*",          # ОБЯЗАТЕЛЬНО: текст сообщения
    format="markdown",             # Опционально: "markdown" или "html"
    attachments=[{                 # Опционально: файлы, изображения
        "type": "image",
        "url": "https://example.com/image.jpg"
    }]
)
```

## Обработка ошибок

### Иерархия исключений:
```python
MAXAPIException                    # Базовое
├── AuthenticationError (401)      # Неверный токен
├── BadRequestError (400)          # Неверный запрос
├── NotFoundError (404)            # Не найдено
├── RateLimitError (429)           # Превышен лимит
└── ServiceUnavailableError (503)  # Сервис недоступен
```

### Обработка:
```python
from max_api.exceptions import MAXAPIException, RateLimitError

try:
    client.send_message(chat_id=123, text="Hi")
except RateLimitError as e:
    print(f"Превышен лимит запросов: {e}")
    time.sleep(60)  # Ждём минуту
except MAXAPIException as e:
    print(f"Ошибка API: {e}")
```

## Окружение и установка

### Установка (Linux):
```bash
# ВСЕГДА используй venv на Linux
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Файл .env:
```bash
MAX_BOT_TOKEN=your_token_here
```

### Запуск примеров:
```bash
source venv/bin/activate
python examples/echo_bot.py
```

## Тестирование

### Запуск тестов:
```bash
source venv/bin/activate
pytest tests/ -v
```

### Создание новых тестов:
- Используй `responses` для мокирования HTTP
- Тестируй все edge cases
- Проверяй правильность query-параметров в URL

## Документация

### При создании новых фич:
1. Обнови `README.md`
2. Создай пример в `examples/`
3. Добавь тесты в `tests/`
4. Обнови `DOCS_INDEX.md`

### Важные документы:
- `README.md` - главная документация
- `DEVELOPMENT_PLAN.md` - план разработки
- `ALL_FIXES.md` - все исправления
- `LONG_POLLING_TIMEOUT.md` - про timeout
- `HOW_TO_CONNECT_BOT.md` - подключение бота
- `START_HERE.md` - быстрый старт

## Rate Limiting

MAX API имеет лимит **30 RPS** (запросов в секунду).
- `RateLimiter` в `max_api/utils.py` автоматически контролирует это
- **НЕ** удаляй RateLimiter из кода
- При 429 ошибке - жди перед повтором

## Production vs Development

### Development (Long Polling):
```python
# Для разработки и тестирования
updates = client.get_updates(timeout=30, marker=last_marker)
```

### Production (Webhook):
```python
# Для production
client.create_subscription(url="https://your-server.com/webhook")
```

## Частые ошибки

### ❌ "Unknown recipient"
**Причина**: отправляешь chat_id в body вместо query
**Решение**: используй `?user_id={chat_id}` в URL

### ❌ TimeoutError при Long Polling
**Причина**: думаешь, что это ошибка
**Решение**: это нормально! Просто `continue` в цикле

### ❌ "command not found: python"
**Причина**: не активирован venv
**Решение**: `source venv/bin/activate`

### ❌ "externally-managed-environment"
**Причина**: системный Python на Linux
**Решение**: используй venv (см. `INSTALL_LINUX.md`)

## Контрольный список изменений

Перед коммитом проверь:
- [ ] Токен не закоммичен (должен быть в `.gitignore`)
- [ ] Все примеры работают
- [ ] Тесты проходят (`pytest tests/ -v`)
- [ ] Документация обновлена
- [ ] Long Polling timeout обработан правильно
- [ ] chat_id = sender.user_id (не recipient.chat_id!)
- [ ] user_id в query параметрах при отправке

## Полезные команды

```bash
# Информация о боте
python quick_bot_info.py

# Получить chat_id пользователя
python examples/get_chat_id_bot.py

# Запустить эхо-бота
python examples/echo_bot.py

# Запустить бота с командами
python examples/simple_bot.py

# Консольное приложение
python -m console_app.main

# Тесты
pytest tests/ -v

# Установка в dev режиме
pip install -e .
```

## База знаний MAX API

### API Endpoints:
- Base URL: `https://platform-api.max.ru`
- GET `/me` - информация о боте
- POST `/messages?user_id={id}` - отправка сообщения
- GET `/updates?timeout={sec}&marker={marker}` - получение обновлений
- POST `/subscriptions` - создание webhook
- GET `/subscriptions` - список webhook'ов
- DELETE `/subscriptions/{id}` - удаление webhook

### Update Types:
- `message_created` - новое сообщение
- `message_updated` - сообщение изменено
- `message_deleted` - сообщение удалено
- `callback_query` - нажатие на inline кнопку

### Message Format:
- `markdown` - поддержка Markdown форматирования
- `html` - поддержка HTML тегов
- По умолчанию: plain text

---

## 🎯 Главное правило

**Если не уверен - читай исходники в `max_api/client.py` и примеры в `examples/`!**

Все актуальные исправления задокументированы в `ALL_FIXES.md`.
